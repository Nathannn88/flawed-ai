# 《诗人与岛》协作与文件使用说明

## 1. 现在以哪里为准

仓库内文件是唯一有效工作源：

1. `design/00-项目核心/最高设计权威.md`：定义项目为什么存在、核心循环、设计护栏和统一术语。
2. `design/设计资料索引.md`：列出当前有效设计文稿与美术源资产。
3. 对应专题文稿：承载玩法、世界观、剧情、角色和视觉细节。
4. `记录/进度.md`：只记录当前状态、待办和开放问题。
5. `记录/决策与活动日志.md`：append-only，保存人做出的决定与项目活动。

Obsidian 已不再承担设计总览或唯一编辑源。Obsidian 笔记、聊天附件、会议转写、Google Drive 文件等可以作为输入与历史证据；一旦内容被项目采纳，应整理进仓库对应文件，并在 `记录/源文件索引.md` 说明来源和版本关系。

## 2. 新的协作方式

用户把资料放入项目目录后，只需告诉 Codex：

- “我更新了 `某个路径`，读取一下”；
- “新素材放在 `某个目录`，按现有规则整理”；
- “这份新设定替代旧设定，检查影响范围”。

Codex 随后：

1. 读取用户指定的原文件；
2. 判断它属于定案、提案、参考素材还是待确认输入；
3. 对照最高设计权威做一致性与目标检查；
4. 整理到正确文稿或素材目录；
5. 更新索引、进度、开放问题和活动日志；
6. 若发生路径变化，修复规则、skill 配置和全部有效引用；
7. 对被替代文件先进入待删除审核，不直接永久删除。

## 3. `design/` 的职责边界

| 位置 | 承载内容 |
|---|---|
| `00-项目核心/` | 最高设计权威与全局原则 |
| `10-玩法与系统/` | 核心循环、机制、商业与社群规则 |
| `20-世界观与剧情/` | 世界观、故事、事件与叙事 |
| `30-角色设定/` | 诗人、宠物、上贡者等角色资料 |
| `40-美术素材/` | 建模、角色、场景、道具、供品等源资产 |
| `50-视觉规范/` | 色彩、字体、构图、动效和设计 token |
| `90-待整理/` | 暂时无法可靠归类的输入 |
| `99-待删除审核/` | 已失效但尚未获准永久删除的旧件 |

法律材料继续放在 `法律文件/`，不与产品设计混放。进度、决策、来源关系继续放在 `记录/`。

## 4. 设计素材与代码素材

`design/40-美术素材/` 保存原始、高质量、可继续编辑的设计源资产。代码不得直接读取这里。

正式运行需要某份素材时：

```text
design/40-美术素材/源文件
  → 选用、导出、压缩或格式转换
  → project/public/assets/ 或 project/src/assets/
  → 在素材索引登记“源文件 ↔ 运行时副本”
```

这样设计目录可以继续重构，代码构建也不会被美术归档动作意外破坏。

## 5. 旧文件如何退场

判定某文件失效时，按以下顺序处理：

1. 找到其替代文件或说明为什么已不需要；
2. 检查是否仍被有效文稿、skill、脚本或代码引用；
3. 移入 `design/99-待删除审核/`；
4. 更新 `待删除审核清单.md`；
5. 用户审核后再永久删除。

Git 历史负责长期保存沿革，不要求工作区永久堆放所有过时副本。

## 6. 当前代码状态

- `prototype/temple-mvp/`：独立、可扔掉的 Three.js 验证原型。
- `project/`：较早阶段的 Vite + React + TypeScript 骨架。
- 两者都不是 v3.1 正式实现的默认基础。

下一步不是立刻扩写旧代码，而是先完成最小纵向切片规格，再决定复用、部分重写或归档。

## 7. 每次交付最少检查

- 最高权威与专题文稿是否冲突；
- 新名词是否统一；
- 不确定项是否被明确标成开放问题；
- 所有索引路径是否真实存在；
- 是否还残留旧目录引用；
- 代码是否直接依赖 `design/`；
- 被替代文件是否进入审核而非永久删除；
- 根 `AGENTS.md` / `CLAUDE.md` 与项目级 skill 镜像是否保持语义一致。
