# 写作方式说明 —— Flawed AI 协作工作手册

> 这份文件是给**你（人）**看的，讲清楚"现在该怎么跟 Claude 协作"。
> 给 Claude 看的操作指令在各 `CLAUDE.md` 里；这里讲的是**为什么这么安排、流程长什么样、历史上踩过什么**。
>
> 最后整理日期：2026-06-10

---

## 一、一句话定位

**当前项目处于 v3「设计定稿」阶段，一行 v3 代码都还没写。**
所以现在跟 Claude 的协作 = **一起把游戏设计想清楚、写清楚**，不是写代码。

---

## 二、整理后的架构（这次清理的结果）

过去这套协作体系是为 v1 的"编码阶段"搭的，堆了三套互相重叠的流程。这次按**项目阶段**重新分层：

```
┌─ 当前活跃：设计协作层 ───────────────────────────┐
│  design/  ← 你在 Obsidian 写设计                  │
│  CLAUDE.md 的「设计协作循环」← Claude 怎么配合你    │
│  记录/ ← 进度·决策·活动·源文件索引                  │
│  项目主旨.md ← 最高权威，一切以它为准               │
└──────────────────────────────────────────────┘
              ↓（v3 设计定稿、决定写代码时才解冻）
┌─ 冻结待激活：代码工作层 ❄️ ──────────────────────┐
│  根 CLAUDE.md 的「附录」← 编码工作流（已标注冻结）  │
│  project/  ← 现在还是 v1 代码                      │
│  apply-design / check / deploy / feature-pipeline │
│  PLAN.md ← v1 旧规划                              │
└──────────────────────────────────────────────┘
```

**核心原则**：现在读 `CLAUDE.md`，重点只在"当前阶段"和"设计协作循环"两节；附录里的代码流程一律先不碰。

---

## 三、当前阶段怎么协作（设计循环）

```
你在 Obsidian 写 / 改 design/ 里的设计稿
   → 对 Claude 说"读取更新"
   → Claude 做四件事：
        ① 检查与 项目主旨.md 是否一致（含术语统一）
        ② 审慎挑战 —— 设定有漏洞 / 矛盾 / 绕远就直说，给更优方案
        ③ 帮你结构化 —— 零散口述 → 对应子项目下的清晰文稿
        ④ 把没定的事记进 记录/进度.md「开放问题」，不擅自编造
   → 更新 design/ 文稿 + 记录/进度.md；重大决策记 记录/决策与活动日志.md
```

**四个设计子项目分工**（细节见各 `design/*/CLAUDE.md`）：

| 子项目 | 写什么 |
|--------|--------|
| `story/` | 世界观、Art 岛传说 vs 真实历史、三阶段、事件剧本 |
| `character/` | 诗人之像（玩家）、诗人本人、宠物（可变形后代） |
| `system/` | 供奉→转化→再创作、多媒介载体、能量、付费、社区 |
| `style/` | 视觉风格（当前优先级最低，等设定定稿再大改） |

> 写长篇设计稿、需要先理清思路时，可以让 Claude 用 `vibe-writing`；
> 要拆解某个现象 / 帮你做决策时，用 `/my-deep-analyst`（说"分析一下""该不该"会自动触发）。

---

## 四、历史沿革 —— 为什么之前那么乱（你要的"使用经历说明"）

你记忆里"一会儿用 pipeline、一会儿用别的架构"，不是错觉。下面是这套协作体系的来龙去脉：

### 1. 游戏 vision 换过两次（v1 → v2 → v3）

- **v1**：AI 诗人聊天应用 —— 海难失忆诗人、荒岛→出海、企鹅宠物、灯塔社区、纯对话驱动。
- **v2（2026-04-24）**：AI 宠物艺术教育游戏 —— 玩家是被供奉的"诗人之像（空白容器）"、庙宇、可变形宠物、供奉→转化→再创作、艺术漂流网络。
- **v3（2026-06-10 起，当前）**：按 06-09 团队会议重构 —— 诗人乘船离开（未死）、精神存于神像、玩家=神像化身、白天石像/夜晚灵体、4+1 岛屿、上贡者稀有度、解锁式进度、三阶段重排。

设计文档已经换成 v3，但 **`project/` 里的代码还是 v1**。这是最大的一处"淘汰却没清掉"——这次已在 `project/CLAUDE.md` 顶部明确标注。

### 2. 同时存在三套"规划 / 编排"流程，互相重叠

这是"一会儿 pipeline 一会儿别的"的真正来源：

| 流程 | 它是什么 | 现状 |
|------|---------|------|
| `PLAN.md` 的 6-Agent + Phase 0~5 | 早期想用"agent team 分工 + 阶段"来推进 | v1 规划，已冻结 |
| `/my-feature-pipeline` 四阶段 | 后来想用"功能流水线"逐个功能推进 | 自带 checklist 机制，结构可复用 |
| 根 `CLAUDE.md` 的开发工作流 | 又简化成 design→apply→check→commit | 当前已降级为附录 |

三者目标重叠（都是"怎么把活干完"），却没人整合。这次的处理：**不强行三选一**，而是按阶段把它们全部收进冻结附录，等真正写 v3 代码时再选一套激活。

### 3. 一批"幽灵架构"——设计了但从没用起来

- **`design/*/output/` 子目录**：每个子项目 CLAUDE.md 都写了"output/ 存 Claude 加工产物"，但磁盘上根本没建过。已在文档里标注"未启用，需要时再建"。
- **一堆发布 / 安全 skill**（`/canary` `/ship` `/freeze` `/guard` 等）：尚未上线，全程空转。已收进附录不再摆在显眼处。

### 4. 死引用

- `apply-design` skill 的映射表引用 `character/企鹅设定.md` —— 这文件早已改名 `宠物设定.md`，是死链。
- `project/CLAUDE.md` 模块表整张都是 v1 模块（penguin / fuel / spark / familiarity / GLM-5 聊天）。

这两处都已在对应文件里标注为"v1 死引用，v3 重构时重写"。

---

## 五、各文件 / Skill 现在的定位速查

| 文件 / Skill | 现在的角色 |
|------|-----------|
| `design/项目主旨.md` | 🟢 最高权威，一切以它为准 |
| `记录/进度.md` | 🟢 每次新对话先读，现状板（状态/待办/开放问题） |
| `记录/决策与活动日志.md` | 🟢 append-only 时间线，重大决策 + 活动产出 |
| `记录/源文件索引.md` | 🟢 指向 Obsidian / repo 内源文件的单一入口 |
| 根 `CLAUDE.md` | 🟢 操作总入口（当前只看前半段） |
| `design/CLAUDE.md` + 4 个子项目 CLAUDE.md | 🟢 设计协作的分工与规则 |
| `写作方式说明.md`（本文件） | 🟢 协作手册（人读） |
| `/startup` `/my-deep-analyst` `vibe-writing` | 🟢 当前阶段可用 |
| `PLAN.md` | ❄️ v1 规划，冻结待重写 |
| `project/` 全部 | ❄️ v1 代码，待 v3 重构 |
| `apply-design` `check` `deploy` `my-feature-pipeline` | ❄️ 编码阶段才解冻 |
| `/canary` `/ship` `/freeze` 等发布类 | ❄️ 上线阶段才用 |

🟢 = 当前用 　❄️ = 冻结，v3 编码时解冻

---

## 六、什么时候"解冻"代码流程

当 v3 设计定稿、你决定开始写代码时，做这几件事即可切回编码模式：

1. 按 v3 设计**重写** `project/CLAUDE.md` 模块表、`apply-design` 映射表、`PLAN.md`、根 `CLAUDE.md` 附录。
2. 从那批冻结 skill 里**选一套**推进方式（建议保留 `/check` 做质量链，`/my-feature-pipeline` 做功能流水线，放弃 PLAN.md 的 6-Agent 旧规划）。
3. 在 `记录/进度.md` 标记阶段从"设计定稿"切换为"v3 编码"。

> 在那之前，保持简单：**写设计、说"读取更新"、让 Claude 帮你想清楚。**
