# 主管使用说明 — 怎么继续推进这个项目

> 写给**项目主管（你）**的操作手册：每次坐下来该干什么、文件在哪、怎么指挥 Claude、现在卡在哪、下一步往哪走。
> 这是「怎么做事」的手册；「项目是什么」看 `design/项目主旨.md`（最高权威）。

---

## 0. 一句话定位

**Flawed AI** 是一款 AI 宠物艺术教育游戏：玩家=庙宇里的**神像（诗人化身）**，白天石像收贡、夜晚灵体加工，指挥可变形宠物完成「供奉 → 转化 → 再创作」。
**当前阶段：v3 设计定稿中，代码层未启动。** 现在的工作是**设计协作**，不是编码。

---

## 1. 每次开工：三步

1. **新对话先说 `/startup`** —— Claude 自动读进度、报状态、问下一步。
2. **看一眼现状板** `记录/进度.md`（当前在哪 / 待办 / 开放问题）。
3. **告诉 Claude 你今天要干嘛**（改设计 / 答开放问题 / 整理笔记 / 启动 demo……）。

> 长对话里出现重要原始需求或关键方案时，手动敲 `/my-snapshot` 存快照，防止对话被压缩后丢失。Claude 不会自动调。

---

## 2. 项目地图：哪个文件干什么

| 你想要… | 去哪 |
|---------|------|
| 最高权威，一切设计以它为准 | `design/项目主旨.md` |
| 当前进度 / 待办 / 开放问题（**新对话先读**） | `记录/进度.md` |
| 历史决策与活动时间线（只增不改） | `记录/决策与活动日志.md` |
| 外部源文件入口（Obsidian 会议转写、盈利等） | `记录/源文件索引.md` |
| 阶段路线骨架（Phase 0→5） | `PLAN.md` |
| 协作方式全文手册 | `写作方式说明.md` |
| **本手册** | `主管使用说明.md` |
| 各板块细则 | 各子文件夹自己的 `CLAUDE.md` |

**板块架构**：项目按板块分，每块=独立子文件夹+各自 `CLAUDE.md`。根 `CLAUDE.md` 只管全局，**要动哪个板块先读那个板块的 `CLAUDE.md`**。

| 板块 | 子文件夹 |
|------|----------|
| 设计层总入口 | `design/` |
| 世界观 / 叙事 | `design/story/` |
| 角色（神像 / 宠物 / 上贡者） | `design/character/` |
| 系统机制 / 核心循环 | `design/system/` |
| 视觉风格 / 设计 token | `design/style/` |
| 代码层（v1 待重构，冻结） | `project/` |

---

## 3. 设计协作循环（当前核心工作流）

```
你在 Obsidian 写/改设计 → 对 Claude 说"读取更新"
  → Claude：读变更 → 审慎挑战 → 结构化 → 检查与项目主旨一致 → 更新 design/ 文稿 + 记录/进度.md
```

**Claude 在设计阶段不只是录入，它会**：
1. **检查一致性** —— 与 `项目主旨.md` 冲突？术语统一吗（主旨第十一节术语表）？
2. **审慎挑战** —— 设定有漏洞/矛盾/绕远，直接提更优方案，不盲从。
3. **结构化** —— 零散口述 → 对应板块下的清晰文稿。
4. **标记开放问题** —— 没定的记进 `记录/进度.md`「开放问题」，不擅自编造。

> 用法提示：要 Claude 帮你**做决策/破局**，触发词"破局""该不该""要不要"会自动调深度分析；多源**调研**说"研究一下"；长文**写作**用 `vibe-writing`。

---

## 4. 记录铁律（每次都守）

| 写什么 | 写到哪 |
|--------|--------|
| 当前状态 / 待办 / 开放问题（会被覆盖的现状） | `记录/进度.md` |
| **重大决策**（谁拍板 + 为什么 + 否决了什么） | `记录/决策与活动日志.md` 标 `[决策]` |
| **干了什么 / 产出哪些文件**（Obsidian 笔记只链接不复制） | `记录/决策与活动日志.md` 标 `[活动]` |

- 决策主体是**人**（你和队员），记清"谁定的、为什么、放弃了什么"。
- 落 Obsidian 的产物，在 `[活动]` 提一嘴 + 链接，别脱档。

---

## 5. Obsidian 笔记规范（硬规则）

Claude 给本项目生成**任何** Obsidian 笔记时必守（已写进根 `CLAUDE.md`）：
1. frontmatter 必带 `project: "[[神像供奉AI]]"`（属性 + 双链，**不是 tag**）——用它把项目笔记归集到枢纽笔记。
2. tag **极简**：1-2 个能一眼分类即可，优先复用项目已用 tag（如 `游戏设计`），不堆宽泛/不相关 tag。
3. 原则：Obsidian 源文件**不复制进 repo**，`记录/源文件索引.md` 只链接（避免两份失同步的真相）。

---

## 6. 安全红线 & Git

- **红线**：① 不访问 `D:\保存文件\claude库\` 以外的文件（Obsidian 除外）；② **不自动 git push**（须你拍板）；③ API 密钥走 `.env.local`，禁止硬编码。
- **commit**：Claude 可以提交（`type: english description`，当前阶段多 `docs:` / `design:`），**push 必须你确认**。
- **父仓库不碰子仓库**：`flawed-ai` 是独立仓库（remote `Nathannn88/flawed-ai`），别在父仓库 `claude库` 里 add 它。

---

## 7. 现在在哪 · 下一步（截至 2026-06-16）

**阶段：PLAN.md Phase 0（设计定稿），代码未启动。**

**眼下最该推的三件事**（详见 `记录/进度.md` 待办）：
1. **答开放问题，尤其 #1「资源体系统一」** —— 智慧能量/审美能量/信仰值/代币 四者关系，**卡住所有系统数值**，不定案下游无法量化。
2. **核实开放问题 #9「岛屿字母编号」** —— 小问题但会让美术分工踩坑，最快能清掉。
3. **启动「初创版」最小 demo**（PLAN Phase 2）—— 它**故意和未定数值解耦**，是纯视觉骨架，所以 #1 没定也能先搭。**待你拍板技术栈与范围。**

> **关于「初创版」的待决岔路口**（开工前必须你定）：
> - **技术栈**：网页 3D（React Three Fiber，最适合"你写码+Claude 实现"、可上 Vercel）/ Unity / Godot。
> - **范围**：建议先做"纯视觉骨架"——开门→村民走来→关门切夜→宠物变形加工→出成品，**AI 讲解先用占位文本**，不碰数值/信仰/抽卡。
> - **v1 处置**：`project/` 是 v1 网页应用（与 v3 几乎不可复用），建议整体归档后从零起骨架，而非缝补。

---

## 8. 开放问题速览（9 项，详见 `记录/进度.md` 及 Obsidian「神像供奉游戏 - 开放问题清单」）

| # | 问题 | 挡不挡开发 |
|---|------|-----------|
| 1 ⭐ | 资源体系统一 | **挡**（卡所有数值） |
| 2 | 昼夜跳过/托管形态 | 影响留存 |
| 3 | 白天/黑夜状态命名 | 否 |
| 4 | 「媒体与综合捣乱」线整合 | 否（第三阶段） |
| 5 | 成品稀有度方案 | 影响美术量 |
| 6 | 萨满五子设定（注：文稿扩写，待团队确认采纳） | 否（背景） |
| 7 | v2「空白容器反讽」是否保留 | 否（哲学层） |
| 8 | 盈利模式按 v3 复核 | 否 |
| 9 | 岛屿字母编号不一致 | 易踩坑（美术分工） |

---

## 9. 团队分工（06-09 会议定）

| 任务 | 负责人 |
|------|--------|
| 岛民人物设计 | 王欣然 |
| 四岛符号设计 | 李宇哲 |
| 美术总监（统一风格） | 待招（已有人选） |
| 最小 demo / 代码 | 你本人 |

**美术取向**：倾向 2D 动画、避免廉价 3D；但 demo 先用 3D 模型试做（模型可复用），场景固定视角。

---

## 10. 三阶段大方向

1. **单机养成**（当前核心）→ 2. **社区型**（玩家互访、社交化）→ 3. **世界观 + 故事线**。
可能额外做深度绑定的社交软件。团队认为终局做成社区/社交媒体的潜力 > 单纯一个游戏。

---

## 附：代码工作流（冻结 ❄️）

v3 无代码，整套编码工作流当前不激活，原 4 个 v1 编码 skill 已删。
**v3 编码启动时**：按新设计重写 `project/CLAUDE.md` 与 `PLAN.md` 的 Phase 2/3 细节，重建编码/质量链/部署 skill；`/ship` 等的 push 仍须你确认。
