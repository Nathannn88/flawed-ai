# 《诗人与岛》（Insula Artis）

> 对外中文名为《诗人与岛》，拉丁文工作名为 *Insula Artis*；Flawed AI／有缺陷的 AI 是内部项目与仓库代号。
> 当前核心体验：玩家作为诗人的神像化身，通过白天对话获得记忆能量，夜晚将供品转化为记忆承载器并与艺术品共鸣，逐步形成可修改、可分享的个人艺术家画像。

---

## 当前阶段

**v3.1 · 持续可玩原型开发与最小纵向切片验证中 · 正式工程迁移待定。**

`prototype/temple-mvp/` 已由一次性验证件升格为**持续演进的可玩产品原型**，当前功能和画面从这里继续推进；它还不是最终生产架构。`project/` 仍是较早的 Vite 骨架，后续需单独决定迁移、重写或归档，不因目录名含 `mvp` 而继续降低质量标准。

| 目的 | 权威位置 |
|---|---|
| 最高设计权威 | `design/00-项目核心/最高设计权威.md` |
| 全部有效设计与素材导航 | `design/设计资料索引.md` |
| 当前进度、待办、开放问题 | `记录/进度.md` |
| 决策与活动历史（append-only） | `记录/决策与活动日志.md` |
| 资料来源与版本关系 | `记录/源文件索引.md` |
| 协作方式手册 | `写作方式说明.md` |
| 阶段路线 | `PLAN.md` |

---

## 基本规则

- **语言**：交互用中文；commit message 用英文。
- **设计源**：仓库内 `design/` 是产品设计文稿与美术源资产的唯一有效源。Obsidian、Google Drive、聊天附件和临时目录只能作为输入或历史证据，不能与仓库形成并列真相。
- **代码隔离**：运行时需要的美术资产必须复制或转换到当前活动代码目录（现为 `prototype/temple-mvp/assets/`；正式工程迁移后改为 `project/`）；代码不得直接依赖 `design/` 路径。
- **目录可变**：用户可调整 `design/` 分类。迁移后必须同步更新索引、规则、记录、skill 配置及全部有效引用。
- **已退役导航与规则**：不再创建或恢复 `.base`、`*MAP.md`、`design/` 子板块或历史版本专属 `AGENTS.md`／`CLAUDE.md`；设计导航统一由 `design/设计资料索引.md` 承担，设计规则只保留 `design/` 根目录一对镜像。
- **旧件处理**：疑似淘汰文件先移入 `design/99-待删除审核/`，列明理由和替代物；只有用户再次确认后才永久删除。
- **安全**：不自动 push；API 密钥只放 `.env.local`，禁止硬编码。
- **Session 启动**：新对话执行 `/startup`，先读进度和近期决策。
- **Git commit**：格式为 `type: english description`；大改动前可先做 `checkpoint:`。任何 commit/push 都要附 annotated tag；push 必须先经用户确认。
- **规则镜像**：同目录 `CLAUDE.md` 与 `AGENTS.md` 保持业务语义一致。只允许宿主名、显式 skill 调用语法和真实工具入口不同；修改后运行 `skill-stocktake`。

---

## 执行与协作规则

- **清晰目标直接推进**：需求目标和授权边界明确时，持续执行到可验证结果，不把本可从仓库判断的问题反复抛回用户；只有会改变产品方向、权威设定、法律口径、永久删除或对外发布的选择才停下确认。
- **长任务保持可恢复**：多阶段任务持续更新 `.agent/long-task/STATE.md`，记录当前动作、已验证证据、阻塞和准确恢复入口；新会话由 `/startup` 先恢复未完成验收。
- **过程透明**：长时间执行期间主动汇报当前阶段、关键假设和阻塞；最终交付必须区分 `PASS`、`BLOCKED` 与“尚未验证”，不能把实现完成写成验收完成。
- **新决定覆盖旧框架**：用户最新明确决定若推翻现有阶段、术语或实现假设，先重启诊断，再同步所有活动规则、计划、进度、长期任务契约和相关 skill；历史日志保留原貌并追加更正。
- **视觉证据属于交付物**：UI／3D／美术相关任务的截图、录屏和分析报告不是可选装饰；证据应按 `prototype/temple-mvp/视觉与交互验收规范.md` 生成并由 Agent 实际查看。

---

## 目录与职责

| 目录 | 作用 |
|---|---|
| `design/00-项目核心/` | 最高权威、项目目标与总原则 |
| `design/10-玩法与系统/` | 核心循环、机制、数值、商业与社群规则 |
| `design/20-世界观与剧情/` | 世界观、剧情、事件与叙事素材 |
| `design/30-角色设定/` | 诗人、宠物、上供者及角色关系 |
| `design/40-美术素材/` | 建模、场景、角色、道具与供品等美术源资产 |
| `design/50-视觉规范/` | 色彩、字体、构图、动效、设计 token 等视觉规则 |
| `design/90-待整理/` | 暂时无法可靠归类的新输入 |
| `design/99-待删除审核/` | 已判定失效、等待用户最终删除确认的旧件 |
| `法律文件/` | 合同、著作权登记及其他法律文档；不放进 `design/` |
| `记录/` | 进度、决策活动时间线、来源与版本关系；`v1归档/` 只保存退出活动区的历史证据 |
| `prototype/temple-mvp/` | 当前持续演进的可玩产品原型；功能、交互和视觉验收从这里继续 |
| `project/` | 代码及运行时资产；当前仍属较早验证骨架 |

`design/` 只在根目录维护一对 `AGENTS.md` / `CLAUDE.md`。易变的分类目录不再各自放规则文件，以免每次重构目录都产生死引用。

`记录/v1归档/` 不属于活动规则、有效设计源或代码依赖；其中不得出现 `AGENTS.md`／`CLAUDE.md`，也不得被运行时代码、构建、测试或 Skill 活动输入引用。

---

## 当前设计协作循环

```text
用户把新资料放入项目文件夹并告知具体路径
  → 读取原文件并判断类别、权威级别与替代关系
  → 审慎挑战：检查目标偏移、设定冲突、遗漏与更低成本方案
  → 更新对应设计文稿和素材索引
  → 更新进度、开放问题与 append-only 决策/活动日志
  → 检查所有有效路径与依赖
```

执行设计变更时必须：

1. 对照 `design/00-项目核心/最高设计权威.md` 与其中术语。
2. 不把未确认内容伪装成定案；不确定项进入 `记录/进度.md` 的开放问题。
3. 新素材至少登记来源、类型、用途、状态和运行时复制位置。
4. 若新内容替代旧内容，明确旧文件去向，不静默覆盖历史。
5. 若目录或文件改名，全文搜索并修复所有有效引用。

---

## 记录规则

| 信息 | 写入位置 |
|---|---|
| 当前状态、待办、开放问题 | `记录/进度.md` |
| 人做出的重大决定、原因及被否决方案 | `记录/决策与活动日志.md`，标 `[决策]` |
| 本次做了什么、产出或迁移了哪些文件 | `记录/决策与活动日志.md`，标 `[活动]` |
| 外部输入、历史版本与派生文件关系 | `记录/源文件索引.md` |

长对话产生关键原始需求或方案时，提醒用户用 `/my-snapshot` 保存需求快照；不自动调用。

---

## 验证与成本门卫

- 设计、规则和记录类任务默认采用机械检查，并按残余风险至多追加一次独立一致性/反例审查。
- 追加验证前必须列明已有证据、未覆盖风险、验收条件与停止条件；已有证据直接复用。
- 连续一轮没有发现会改变产物或结论的实质问题，立即停止同类审查。
- **视觉交付不能只验逻辑**：凡修改 UI、3D 场景、模型、材质、构图、动效或交互反馈，完成前必须运行真实页面，主动截取代表状态并亲眼分析构图、遮挡、层级、色彩、画风一致性、可读性与交互暗示；涉及旋转、变形、昼夜切换等动态表现时，优先录制短屏或至少连续截图检查时间变化。
- 截图／录屏必须覆盖本次新增路径和最相关回归状态；生成文件但未实际查看不算验收。浏览器或录屏能力不可用时明确记为 `BLOCKED`，不得用 DOM、坐标或静态测试冒充视觉通过。

---

## 当前代码状态

`prototype/temple-mvp/` 的功能迭代已解冻并持续推进；`project/` 的正式工程迁移尚未启动。

1. 当前新增功能在活动原型中实现，并同时补齐可执行规格和自动测试；
2. 每次 UI／3D 交付按 `prototype/temple-mvp/视觉与交互验收规范.md` 主动截图、按需录屏并分析，不得把“能运行”等同于“画面合格”；
3. `project/` 迁移前需决定旧 Vite 骨架的复用／归档边界，并重写模块表、质量链和部署流程；`temple-mvp` 只是历史目录名，改名或迁入 `project/` 另作工程决策，不阻塞当前迭代；
4. 将选定设计源资产复制／转换到活动代码目录并保持来源映射；禁止代码直接读取 `design/`。

历史细节见 `写作方式说明.md` 与 Git 历史。
