# Flawed AI — 有缺陷的AI

> 一款以提升用户艺术感知能力为核心目标的 **AI 宠物艺术教育游戏**。
> 玩家扮演供奉于庙宇中的**神像（诗人的化身）**：白天石像收贡、夜晚灵体加工，指挥可变形宠物完成「供奉 → 转化 → 再创作」。

---

## ⬛ 当前阶段（先读这里）

**版本 v3 · 设计定稿中 · 代码层尚未启动。**

> 现在的工作是**设计协作**，不是编码——整套代码工作流已冻结（见末尾附录）。

| 看什么 | 去哪 |
|--------|------|
| 最高权威（一切设计以它为准） | `design/项目主旨.md` |
| 当前进度 / 待办 / 开放问题（**新对话先读**） | `记录/进度.md` |
| 决策与活动历史（append-only 时间线） | `记录/决策与活动日志.md` |
| 外部源文件（Obsidian 会议转写 / 盈利等） | `记录/源文件索引.md` |
| 协作方式全文手册（人读） | `写作方式说明.md` |

---

## 基本规则

- **语言**：交互用中文，commit message 用英文。
- **安全红线**：① 不访问 `D:\保存文件\claude库\` 以外的文件（Obsidian 除外）；② 不自动 git push（须用户确认）；③ API 密钥走 `.env.local`，禁止硬编码。
- **Session 启动**：新对话执行 `/startup`（读进度 → 报状态 → 问下一步）。
- **Git commit**：`type: english description`（feat / fix / docs / design / chore…）；大改动前先 `checkpoint:`。当前阶段多为 `docs:` / `design:`。
- **Obsidian 笔记规范**（生成任何本项目 Obsidian 笔记时必守，全局 obsidian skill 也遵此）：① frontmatter 必带 `project: "[[神像供奉AI]]"`（属性 + 双链，非 tag）；② tag 极简——1-2 个能一眼分类即可，优先复用项目已用 tag（如 `游戏设计`），不堆宽泛/不相关 tag。

---

## 板块架构（重要）

项目按**板块**划分，每个板块是一个**独立子文件夹、带自己的 `CLAUDE.md`**。
**根 CLAUDE.md 只管全局；要动哪个板块，先读那个板块的 `CLAUDE.md`**——具体写什么、放哪、用什么术语都在那里，不在这里。

| 板块 | 子文件夹 | 路由 |
|------|----------|------|
| 设计层总入口 | `design/` | `design/CLAUDE.md` |
| 世界观 / 叙事 | `design/story/` | `design/story/CLAUDE.md` |
| 角色（神像 / 宠物 / 上贡者） | `design/character/` | `design/character/CLAUDE.md` |
| 系统机制 / 核心循环 | `design/system/` | `design/system/CLAUDE.md` |
| 视觉风格 / 设计 token | `design/style/` | `design/style/CLAUDE.md` |
| 代码层（⚠️ 仍是 v1，待 v3 重构） | `project/` | `project/CLAUDE.md` |

其余顶层：`记录/`（进度·决策·活动·源文件索引）、`写作方式说明.md`（手册）、`法律文件/`（合伙协议草稿）、`PLAN.md`（v3 阶段骨架）、`design/项目主旨.md`（最高权威）、`design/v1-archive/`（v1 归档·仅参考）。

**工作流方向**：`design/（你在 Obsidian 编辑）→ project/（Claude 实现）→ 部署`。当前只走第一段。

---

## 设计协作循环（当前核心工作流）

```
你在 Obsidian 写/改设计 → 对 Claude 说"读取更新"
  → Claude：读变更 → 审慎挑战 → 结构化 → 检查与项目主旨一致 → 更新 design/ 文稿 + 记录/进度.md
```

**Claude 在设计阶段的职责**（不只是录入）：
1. **检查一致性** — 是否与 `design/项目主旨.md` 冲突？术语是否统一（主旨第十一节术语表）？
2. **审慎挑战** — 设定有漏洞 / 矛盾 / 绕远，直接提更优方案，不盲从。
3. **结构化** — 零散口述 → 对应板块下的清晰文稿。
4. **标记开放问题** — 未定案的记入 `记录/进度.md`「开放问题」，不擅自编造。

### 记录铁律（每次都要守）

| 写什么 | 写到哪 |
|--------|--------|
| 当前状态 / 待办 / 开放问题（会被覆盖的现状） | `记录/进度.md` |
| **重大决策**（你/队员拍板 + 为什么 + 否决了什么） | `记录/决策与活动日志.md` 标 `[决策]` |
| **干了什么 / 产出哪些文件**（含 Obsidian 笔记，只链接不复制） | `记录/决策与活动日志.md` 标 `[活动]` |

- 决策主体是**人**（你和队员），记清"谁定的、为什么、放弃了什么"，不只结论。
- 音频→笔记这类落 Obsidian 的产物，在 `[活动]` 提一嘴 + 链接，别脱档。
- 长对话出现重要原始需求 / 关键方案时，**提醒用户用 `/my-snapshot`**（纯手动，Claude 不自动调用，只提醒）。

---

## Skill 速查（当前阶段）

| 时机 | Skill |
|------|-------|
| 新对话启动 | `/startup` |
| 现象拆解 / 决策破局 | `/my-deep-analyst`（硬触发词见全局偏好） |
| 设计深研 / 多源调研 | `/my-deep-research` |
| 长文设计稿写作 | `vibe-writing` |

---

## 附录：代码工作流（冻结 ❄️）

v3 无代码，整套编码工作流**当前不激活**。原 4 个项目编码 skill（apply-design / check / deploy / my-feature-pipeline）全是 v1 死引用，**已删除**。

**v3 编码启动时**：按 v3 设计重写 `project/CLAUDE.md` 模块表与 `PLAN.md`，并重建编码 / 质量链 / 部署 skill；全局 gstack skill（`frontend-design` / `/investigate` 等）仍可用，`/ship` 的 push 须用户确认。

> 历史沿革见 `写作方式说明.md`；旧工作流细节见 git 历史。
