filters:
  and:
    - file.folder == "Efforts/On/AI Poet Pipeline/design/style"
    - '!file.name.contains("MAP")'
    - file.ext == "md"
formulas:
  类型: if(file.folder.contains("output"), "📦 产出", "📝 文稿")
views:
  - type: table
    name: 视图
