# 待删除审核清单

> 本目录只存放已经退出活动区、但尚未获得永久删除确认的内容。这里的文件不参与当前设计，不被代码引用。
>
> 永久删除前需要向用户逐项说明唯一内容、活动引用、可恢复性和删除理由。

## 已移入本目录

| 类别 | 路径 | 当前判断 | 建议 |
|---|---|---|---|
| Google Drive 旧镜像 | `Google-Drive协作旧件/` | 2026-04 的旧设计导出、导出脚本和局部 pandoc 权限 | 若团队不再使用 Drive 协作则删除 |
| 从 Git 恢复的旧设计 | `从Git恢复的旧设计/` | 已退出活动路径，但恢复实体副本供用户逐项查看；现余 21 个文件 | 审核独有内容后再决定删除 |

## 已获授权并永久删除

| 日期 | 类别 | 删除范围 | 结果与恢复 |
|---|---|---|---|
| 2026-07-30 | 旧 Obsidian 导航 | 旧 `.base`、`*MAP.md` 共 8 个 | 已删除；当前导航由 `design/设计资料索引.md` 取代，可从 Git 历史恢复原版本 |
| 2026-07-30 | 旧分板块规则 | 旧 `AGENTS.md`／`CLAUDE.md` 共 8 个 | 已删除；设计规则统一为 `design/` 根目录一对镜像，可从 Git 历史恢复原版本 |

以上共删除 16 个文件；空的 `Obsidian旧导航/` 与 `旧板块规则/` 同时移除。它们不再属于待审核范围，也不得恢复为活动依赖。

## 从 Git 恢复到审核区的旧设计

> 这些文件在本轮开始前已从活动工作树移除。2026-07-30 为保留“先审核、后删除”门禁，从当前 Git 基线恢复实体副本到本目录；它们仍不参与当前设计。

| 审核区路径 | 原路径 | 内容 | 当前判断 |
|---|---|---|---|
| `从Git恢复的旧设计/v1-archive/` | `design/v1-archive/` | 海难诗人、企鹅、灯塔、旧 UI、角色圣经及 ZIP 等 16 个 v1 文件 | 已被 v3.1 替代，但可能有沿革与视觉参考价值 |
| `从Git恢复的旧设计/style/` | `design/style/` | 视觉风格、设计系统与 Tailwind token，共 3 个历史内容文件 | 不适用于当前产品；其中通用视觉表达可在重建 v3.1 规范前抽取 |
| `从Git恢复的旧设计/初始版内容指导.md` | `design/初始版内容指导.md` | 旧“小麦—面包机—唱片”MVP 规格 | 被“对话—记忆能量—承载器—共鸣—画像”纵向切片替代 |
| `从Git恢复的旧设计/项目主旨.md` | `design/项目主旨.md` | 旧版项目主旨 | 被 `design/00-项目核心/最高设计权威.md` 替代 |

## 尚未纳入本轮删除

| 路径 | 原因 |
|---|---|
| `project/v1-archive/` | 属于代码历史，不在本轮设计目录整理范围；后续单独审查复用价值 |
| 根目录 `sc_*.png`、`test_*.png`、`tmp_*.png` | 可能是旧 UI 验证截图，需先确认是否仍作为回归证据 |
| `法律文件/著作权登记/` 的 v0.9 文件 | 具有作品沿革和确权时间证据，不按普通过时设计删除 |

## 永久删除门禁

- [ ] 当前设计和规则已无活动引用；
- [ ] 唯一内容已迁移或明确放弃；
- [ ] 用户逐项确认删除范围；
- [ ] 解析后的绝对路径仍位于本项目或明确目标目录；
- [ ] 删除后运行全仓残留扫描；
- [ ] 说明能否通过 Git 恢复。
